# v7

*June 2026. Superseded by [`../v8/`](../v8/). Outputs not tracked — see
[why](#why-this-folder-looks-empty).*

A step in the v6 → v12 sequence. Referenced from
[`../HANDOFF_v8_enclosure.md`](../HANDOFF_v8_enclosure.md) and
[`../HANDOFF_v8_validate_and_finalize.md`](../HANDOFF_v8_validate_and_finalize.md)
as the state v8 was built against.

<!-- TODO: v7 shipped no BUILD.md, unlike every other version in this sequence.
     What changed at v7 is recoverable only from the v8 handoffs and from the
     generator. One paragraph from whoever built it would close the gap. -->

## Why this folder looks empty

It held only `.stl`, `.step`, `.png` and `.gcode` — all build outputs, all
gitignored, and it carried no BUILD.md to survive them. The whole folder therefore
disappeared when the design record was committed; this README exists so the
references to it resolve.

The versions either side of it are documented: [`../v6/BUILD.md`](../v6/BUILD.md)
and [`../v8/BUILD.md`](../v8/BUILD.md).
